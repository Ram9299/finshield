name: CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      # Optional: sanity check Docker (Testcontainers needs Docker)
      - name: Docker info
        run: docker version

      - name: Run tests
        working-directory: finshield/finshield
        run: mvn -B test

      - name: Build jar
        working-directory: finshield/finshield
        run: mvn -B -DskipTests package